<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SearchArtistActivityFragment">

    <!--Search Artist Field-->
    <EditText
        android:id="@+id/searchArtistEditText"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:hint="@string/search_artists_edittext_hint"
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:layout_toStartOf="@+id/voiceSearchButton" />

    <at.markushi.ui.CircleButton
        android:id="@+id/voiceSearchButton"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:src="@drawable/ic_mic"
        app:cb_color="#FFFFFF"
        app:cb_pressedRingWidth="2dip"
        android:layout_alignBottom="@+id/searchArtistEditText"
        android:layout_alignEnd="@+id/artistListView">

        <requestFocus />
    </at.markushi.ui.CircleButton>

    <ListView
        android:id="@+id/artistListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/searchArtistEditText"
        android:layout_marginTop="20dp" />

    <ProgressBar
        android:id="@+id/progressBar1"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true" />

</RelativeLayout>
